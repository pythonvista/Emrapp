import { PipeTransform } from '@angular/core';
import * as momentNs from 'moment';
export declare class FormatMomentDatePipe implements PipeTransform {
    transform(value: momentNs.Moment, format: string): string;
}
